# Dragonlair home config for model pulls
# Change just this line to switch default target: amd or nvidia
TARGET_ENV=amd

# Change this line to switch default model file
MODEL_FILE=/home/daravenrk/dragonlair/model-sets/amd-coder-14b.txt

# 1 = skip models already present on the target endpoint
SKIP_EXISTING=1
