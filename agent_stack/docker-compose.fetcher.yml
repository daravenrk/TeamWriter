services:
  fetcher:
    build:
      context: /home/daravenrk/dragonlair/agent_stack
      dockerfile: Dockerfile.fetcher
    container_name: fetcher
    restart: unless-stopped
    command: ["--serve", "--bind", "127.0.0.1", "--port", "11999", "--out-dir", "/app/research"]
    ports:
      - "127.0.0.1:11999:11999"
    volumes:
      - /home/daravenrk/dragonlair/book_project/01_research:/app/research
    environment:
      FETCHER_OUTPUT_DIR: /app/research
      FETCHER_TIMEOUT_SECONDS: "30"
    networks:
      - dragonlair-net

networks:
  dragonlair-net:
    external: true
